apiVersion: v1
kind: Service
metadata:
  name: behemoth-nodejs-auth-service
  namespace: behemoth
spec:
  selector:
    app: behemoth-nodejs-auth
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3010
  type: ClusterIP
