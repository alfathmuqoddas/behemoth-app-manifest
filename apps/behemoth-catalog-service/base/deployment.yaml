apiVersion: apps/v1
kind: Deployment
metadata:
  name: behemoth-nodejs-catalog
spec:
  selector:
    matchLabels: {}
  template:
    metadata:
      labels: {}
    spec:
      containers:
        - name: behemoth-nodejs-catalog
          image: registry.local:5000/behemoth-catalog-service
          ports:
            - containerPort: 3020
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 3020
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 3020
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
          startupProbe:
            httpGet:
              path: /health/startup
              port: 3020
            failureThreshold: 30
            periodSeconds: 10
          env:
            - name: PORT
              value: "3020"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: behemoth-db-password
                  key: DB_PASSWORD
            - name: OMDB_API_KEY
              valueFrom:
                secretKeyRef:
                  name: behemoth-omdb-apikey
                  key: OMDB_API_KEY
          envFrom:
            - configMapRef:
                name: behemoth-db-config
          volumeMounts:
            - name: keys-volume
              mountPath: /app/keys/public.pem
              subPath: public.pem
              readOnly: true
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
      volumes:
        - name: keys-volume
          secret:
            secretName: behemoth-keys
