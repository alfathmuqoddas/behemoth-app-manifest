apiVersion: apps/v1
kind: Deployment
metadata:
  name: behemoth-nodejs-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: behemoth-nodejs-catalog
  template:
    metadata:
      labels:
        app: behemoth-nodejs-catalog
    spec:
      containers:
        - name: behemoth-nodejs-catalog
          image: registry.local:5000/behemoth-nodejs-catalog-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: behemoth-nodejs-catalog-secret
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
