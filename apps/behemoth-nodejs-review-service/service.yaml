apiVersion: v1
kind: Service
metadata:
  name: behemoth-nodejs-review-service
spec:
  selector:
    app: behemoth-nodejs-review-service
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
